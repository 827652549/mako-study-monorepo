---
description: Show JavaScript runtime and package manager versions
---

Please check and display the versions of all JavaScript-related tools installed on this system:

1. **Node.js** - Run `node --version`
2. **npm** - Run `npm --version`
3. **pnpm** - Run `pnpm --version`
4. **bun** - Run `bun --version`

Execute all version checks in parallel for better performance.

Format the output clearly, showing:
- Tool name
- Version number
- Installation status (if not installed, show "Not installed")

Present the results in a clean, readable format (table or list).

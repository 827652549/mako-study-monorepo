## Context

`README.md` 目前为英文，内容包含安装命令、启动命令等基础说明。修改范围仅限该文件。

## Goals / Non-Goals

**Goals:**
- 将 README.md 中的英文自然语言替换为中文

**Non-Goals:**
- 不修改代码块、命令、文件名等非自然语言内容
- 不增加新内容或改变文档结构

## Decisions

无技术决策，直接翻译即可。

## Risks / Trade-offs

- 无风险，纯文档修改，可随时回滚
